# Environment variables for AI Bridal Portrait Generator API

# Required: Google Gemini API key for bridal portrait generation
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Alternative environment variable name (for compatibility with existing frontend code)
API_KEY=your_gemini_api_key_here

# Optional: Custom port for the API server (defaults to 3001)
PORT=3001

# Note: Segmind API keys are provided by clients in request headers
# The server does not need a Segmind API key in environment variables